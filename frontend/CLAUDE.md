@AGENTS.md

<!-- Claude Code reads CLAUDE.md, not AGENTS.md, so this file imports the nested frontend
     AGENTS.md rather than repeating it. Keep it that way — one source, two readers.
     Note: nested CLAUDE.md files are not re-injected after /compact. The reliable mechanism
     for this scope is the path-scoped rule at .claude/rules/frontend.md. -->
